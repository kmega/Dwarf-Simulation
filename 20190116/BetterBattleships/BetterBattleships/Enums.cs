namespace BetterBattleships
{
    public enum CellStatus
    {
        EMPTY = 'e',
        DECK = 'D',
        HIT = 'H',
        MISS = 'M'
    }

    public enum ShipTypes
    {
        Carrier = 5,
        Subarine = 2
    }
}